package demo.reactAdmin.crud.entities.deserializers;

import static org.junit.jupiter.api.Assertions.assertEquals;

import java.io.IOException;

import com.fasterxml.jackson.databind.ObjectMapper;

import org.junit.jupiter.api.Test;

import demo.reactAdmin.crud.entities.Command;

public class CommandDeserializerTest {

    @Test
    public void deserialize() throws IOException {
        // String json = "{\"id\":1}";

        // MyBean bean = new MyBean(1, "My bean");

        // Command command = new ObjectMapper().readValue(json, Command.class);

        // assertEquals("bzsmni", command.reference);
        // assertEquals(16, command.getBananaStore().getBananaCount());
    }
}